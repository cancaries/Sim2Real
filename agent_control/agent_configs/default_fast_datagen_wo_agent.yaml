data:
  data_root: '/home/ubuntu/junhaoge/real_world_simulation/data/waymo_multi_view/031'
  save_dir: '/home/ubuntu/junhaoge/real_world_simulation/data/end2end_map_data/031'
  available_asset_dir: '/home/ubuntu/junhaoge/real_world_simulation/data/assets'
simulation:
  mode: 'datagen' # debug, datagen
  max_steps: 600 # simulation steps
  ego_control_steps: 0 # ego control steps
  FPS: 10 # simulation time step
  scenario_trigger: false # trigger scenario
  ignore_static: false # ignore static objects
  path_ignore_static: true # ignore static objects when spawn agents
  random_seed: 303 # random seed
  agent_spawn:
    spawn_mode: 'random' # random, close_to_ego, same_lane, opposite_lane
    max_spawn_proportion: 0.25 # max spawn proportion of all spawn points
    max_spawn_num: 10 # number of agents to spawn - for every mode
    close_spawn_distance: 30.0 # close spawn distance if spawn_mode is close
ego_vehicle:
  original_step_num: 0 # mantain the original step number
  vehcile_type: 'car' # car
  vehicle_bbox: [2.0, 5.0, 1.8] # width, length, height
  vehicle_fr_length: [2.0, 2.0] # front length/rear length
  vehicle_max_control_angle: 60.0 # control angle
  behavior_type: 'aggressive' # cautiou, normal, aggressive, extreme_aggressive 
other_agents:
  vehicle_type_list: ['car', 'SUV', 'truck', 'bus'] # car, SUV, truck, bus
  vehicle_type_proportion: [0.8, 0.2, 0.0, 0.0] # car, SUV, truck, bus
  exclude_vehicle_type: ['Ferriari_f150','Lamborghini','M1A2_tank','Porsche-911-4s-final','Benz_S','Tesla_cybertruck'] # exclude vehicle type
  vehicle_type_fr_len_dict: # front length/rear length for car, SUV, truck, bus
    car: 1.3
    SUV: 1.5
    truck: 2.5
    bus: 3.0
  vehicle_type_max_control_angle: # control angle for car, SUV, truck, bus
    car: 50.0
    SUV: 50.0
    truck: 40.0
    bus: 35.0
  speed_type: 'fast'
  behaviour_type_proportion: [0.1, 0.7, 0.2, 0.0] # proportion for behaviour types cautiou, normal, aggressive, extreme_aggressive
PID_controller_parameter:
  max_throttle: 0.75 # max throttle value
  max_brake: 0.3 # max brake value
  max_steer: 1.0 # max steering value
  max_steer_ratio: 0.1 # steering ratio
  args_lat_hw:
    K_P: 0.75
    K_D: 0.02
    K_I: 0.4
  args_lat_city:
    K_P: 0.58
    K_D: 0.02
    K_I: 0.5
  args_long_hw:
    K_P: 0.37
    K_D: 0.024
    K_I: 0.032
  args_long_city:
    K_P: 0.15
    K_D: 0.05
    K_I: 0.07

